const login = async (email, password) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

const logout = async () => {
  try {
  } catch (error) {
    console.log(error);
  }
};

const register = async (email, password, username, profileUrl) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

const user = {
  login,
  logout,
  register,
};

export default user;
